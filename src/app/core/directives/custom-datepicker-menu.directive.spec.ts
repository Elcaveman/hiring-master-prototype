import { CustomDatepickerMenuDirective } from './custom-datepicker-menu.directive';

describe('CustomDatepickerMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDatepickerMenuDirective();
    expect(directive).toBeTruthy();
  });
});
